import { Solutions } from '../types'
import { parseInputFile } from '../utils'

const input: string[] = parseInputFile('src/day1/input.txt')
export const day0 = (): Solutions => {
  return { partOne: 'First solution', partTwo: 'Second solution' }
}
